{
  "name": "diagram-tour",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo scaffold for guided tours over Mermaid diagrams.",
  "license": "MIT",
  "packageManager": "bun@1.2.19",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --cwd packages/core build && bun run --cwd packages/parser build && bun run --cwd packages/web-player build && bun run --cwd packages/cli build",
    "test:scripts": "vitest run --config ./scripts/vitest.config.ts --coverage",
    "test:unit": "bun run test:scripts && bun run --cwd packages/core test && bun run --cwd packages/parser test && bun run --cwd packages/web-player test && bun run --cwd packages/cli test",
    "coverage:dashboard": "bun ./scripts/build-coverage-dashboard.ts",
    "test": "bun run test:unit && bun run coverage:dashboard && bun ./scripts/print-coverage-path.ts",
    "coverage": "bun run test",
    "smoke": "bun run build && bun run --cwd packages/web-player smoke",
    "dev": "bun run ./scripts/dev-web-player.ts",
    "dev:open": "bun run ./scripts/dev-web-player.ts --open",
    "dev:interactive": "bun run ./scripts/dev-web-player-interactive.ts",
    "install:cli": "bun run --cwd packages/cli build && npm install -g ./packages/cli",
    "changeset": "changeset",
    "notify:done": "powershell -ExecutionPolicy Bypass -File ./scripts/notify-task-complete.ps1",
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "bun run --filter '*' lint",
    "lint:fix": "eslint . --fix",
    "version:alpha": "node ./scripts/changeset-version.mjs alpha",
    "version:beta": "node ./scripts/changeset-version.mjs beta",
    "version:release": "node ./scripts/changeset-version.mjs release",
    "prepush": "bun run lint && bun run typecheck && bun run test && bun run smoke",
    "prepare": "husky"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.6",
    "@eslint/js": "^9.23.0",
    "@types/node": "^24.5.2",
    "@vitest/coverage-v8": "^3.2.4",
    "eslint": "^9.23.0",
    "eslint-plugin-svelte": "^3.11.0",
    "globals": "^16.0.0",
    "husky": "^9.1.7",
    "svelte-eslint-parser": "^1.1.3",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.28.0",
    "vitest": "^3.2.4"
  }
}
