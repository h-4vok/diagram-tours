# Coverage Dashboard

Canonical command:

```bash
bun run coverage
```

Canonical dashboard:

```text
coverage/index.html
```

Detail reports live under:

```text
coverage/packages/
```

Use the root page first, then drill into a package report when needed.
