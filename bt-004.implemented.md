# BT-004 Design Implemented

This file tracks what has already been implemented during BT-004 and will be removed at the end of the work.

## 2026-03-23

- What changed:
  - implemented Module 1 foundations: global `--font-sans` and `--font-mono` tokens, tap-highlight reset, and consistent code/kbd treatment
  - removed uppercase forcing from technical labels that should read as visual metadata instead of shouting
  - implemented Module 2 foundations: the root shell is full-screen, overflow-hidden, and visually anchored to the redesigned canvas
  - introduced a centered topbar search hint to reinforce the command-palette direction without changing the workflow
  - shifted dark and light theme tokens toward a more graphite/slate visual language and exposed `--bg-base` / `--bg-surface`
  - unified the right-side overlay widths and anchored the zoom/minimap cluster as a single absolute stack
  - corrected the zoom/minimap cluster to float relative to the viewport so it remains visible on large diagrams
- Screens / surfaces affected:
  - topbar
  - global page shell
  - typography system
  - dark and light themes
  - canvas overlay stack
- Notes:
  - Module 1 and Module 2 are now implemented as visible UI foundations, not just token definitions
  - smoke validation was intentionally deferred for later phases because the UI will keep changing
